#include<stdio.h>
int main()
{
int p,i,sum=0,add=0;
scanf("%d",&p);
for(i=1;i<=15;i++)
{
add+=i;
}
printf("the sum of numbers betwwn 1 and 15 is %d",add);
while(p<=45)
{
if(p%3==0)
{
sum+=a;
}
p++;
}
printf("odd numbers between 15 and 45 is %d",sum);
return 0;
}
